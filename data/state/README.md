# data/state/ - start packs

One file per agent, under two pages, current state only. Read at session start, nothing else until a trigger. History goes to data/archive/, never here. Personal and money state live in the private repo (crm-inbox: personal/state/, finance/), not in this folder.

core.md = the PA's pack and the system's living state (replaces the old data/context.md, which is archived at the switch).
